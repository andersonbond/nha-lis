export default function LotManagementPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-[var(--foreground)]">Lot Management</h1>
      <p className="mt-1 text-sm text-[var(--foreground)]/70">
        Coming in MVP 2. Manage lots with list and map view (Mapbox).
      </p>
    </div>
  );
}
